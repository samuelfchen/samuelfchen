{
  "name": "samuelfchen",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "node scripts/generate-seo.cjs && next build",
    "start": "next start",
    "lint": "eslint",
    "deploy": "node scripts/generate-seo.cjs && next build && wrangler pages deploy out --project-name samuelfchen",
    "deploy:dev": "node scripts/generate-seo.cjs && next build && wrangler pages deploy out --project-name samuelfchen-dev"
  },
  "dependencies": {
    "next": "16.2.9",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.9",
    "typescript": "^5",
    "wrangler": "^4.86.0"
  }
}
